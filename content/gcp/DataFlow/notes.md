# DataFlow

* data flow is built upin apache  beam.
#### the four main concepts in Apache Beam are:
* PTransforms
    * the action on the collection are called as Ptransforms
    * it handles of input, transformation and output. 
* PCollections
    * All the data are stored in a distrubed file store called Pcollection. 
    * it is immutable , relate it to something like a dataframe.
    * it doesn't have a limited size and it represents both streaming and batch data.
    * 

* Pipelines 
* Pipeline runners

